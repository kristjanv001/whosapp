import { FoldingCube } from "better-react-spinkit"

export const Loading = () => {
    return (
        <div style={{ display: "flex", justifyContent: "center", alignItems: "center", height: "100vh" }}>

            <FoldingCube color={"white"} size={115} />
        </div>
    )
}